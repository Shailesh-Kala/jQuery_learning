$(document).ready(function () {
  // $("div:lt(2)").css("border", "2px solid red"); // It will select 1-2 rang div
  // $("div:first").css("border", "2px solid red"); // select first div
  $("div:last").css("border", "2px solid red"); //select last div
});
